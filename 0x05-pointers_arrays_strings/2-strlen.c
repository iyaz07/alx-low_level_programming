#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - finds stringlenght of char
 * @s: first param
 * 
 * Return: return is void
 */
int _strlen(char *s)
{
	char str, len;
	len = *s
	
	str = strlen(len);
}
